#pragma once

#include <memory>
#include <sqlite3.h>
#include "CDatabaseManager.hpp"

class CDatabaseQueryWrapper
{
public:
    // void executeSelect(selectQuery *query);

private:
    //std::unique_ptr<CDatabaseManager> m_dbManager = std::make_unique<CDatabaseManager>("test.db");
};